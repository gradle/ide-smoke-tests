plugins {
    id "com.example.commons"
    id "java-library"
    id "com.example.java-dummy-plugin"
}

testing {
    suites {
        integTest(JvmTestSuite) {
            useJUnitJupiter()
            dependencies {
                implementation(project)
            }
        }
    }
}

// TODO (scenario) Groovy precompiled script plugin can reference classes from the same source set
//   Instructions:
//   - Check if the line below has a compiler error in IDEA
println new com.example.SomeType() // Known issue: https://youtrack.jetbrains.com/issue/IDEA-283077